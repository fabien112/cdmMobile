import { NoteParentPipe } from './note-parent.pipe';

describe('NoteParentPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteParentPipe();
    expect(pipe).toBeTruthy();
  });
});
