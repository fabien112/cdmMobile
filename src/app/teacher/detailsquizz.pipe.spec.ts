import { DetailsquizzPipe } from './detailsquizz.pipe';

describe('DetailsquizzPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsquizzPipe();
    expect(pipe).toBeTruthy();
  });
});
